#!/bin/sh

source ./bin/setup

dc build --force-rm

dc up -d web
docker cp app_web:/app/deps/phoenix ./deps
docker cp app_web:/app/deps/phoenix_html ./deps
dc stop

docker build \
    -f Dockerfile.assets \
    -t me_assets \
    .
